# Access Example

Use `access.public_privileged_slot_poisoning.json` as the worked example for:

- privileged slot poisoning
- same-slot joins
- sensitive authorized effects
- two-step ownership-transfer suppression
