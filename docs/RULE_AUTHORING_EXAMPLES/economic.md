# Economic Example

Use `defi.rounding.assumed_actual_balance_mismatch.json` as the worked example for:

- arithmetic facts
- economic effect constraints
- deterministic arithmetic witness generation
