# Delegatecall Example

Use `delegatecall.storage_controlled_target.json` as the worked example for:

- storage-controlled delegate targets
- same-slot join into call target
- ERC-1167 fixed-target suppression
