# Auth Example

Use `auth.authorization_predicate_poisoning.json` as the worked example for:

- whitelist/operator/signer/validator-like state
- authorization predicate modeling
- signature-guard suppression
