# Proxy Example

Use `proxy.unprotected_implementation_upgrade.json` as the worked example for:

- ERC-1967 slot interpretation
- contract-scope storage write predicates
- strong upgrade-guard suppression
