# Init Example

Use `init.public_initializer_takeover.json` as the worked example for:

- binding a written slot
- joining the same slot on a later authorization read
- suppressing one-time initializer guards
