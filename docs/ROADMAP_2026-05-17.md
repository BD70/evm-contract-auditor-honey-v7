# Auditor + rescue engine — roadmap to a "real" rescue bot

_Resume point as of 2026-05-17 19:10 UTC+2. HEAD on `origin/main` before this commit was `aa8b132` (`rescue-prove@5`)._

This doc captures **(a) what's working today**, **(b) the empirical state of the corpus**, and **(c) the ranked next-step plan** to get from "PoE generator that mostly identifies why rescue is impossible" → "professional rescue bot that lands true-positive drains".

---

## 1 · State of the engine (snapshot)

### What works

| Layer | Status | Notes |
|---|---|---|
| Static auditor (Go binary) | live | 14 chains, `evm-audit-capped.sh` wrapper, RSS cap raised 2 GB → **8 GB** this session (was SIGKILL'ing on ~200 Uniswap-V3/Aave-pool sized contracts). |
| Verifiers (`panel/src/server/sim/exploits/*`) | live | `economic-attack@4`, classic call-target rules, reentrancy probe. |
| `rescue-prove@9` | live | Pre-fork short-circuit + evidence-driven admin-name fanout. ~40 % of PoEs short-circuit in 0–10 ms after v9. |
| Broadcaster | live | Envelope-aware pre-flight for flashloan, BigInt-safe JSON, WBNB/WMATIC resolution for `executeRescue` borrow asset. |
| Flashloan receiver | deployed | `FlashLoanRescue.sol` on BSC (chain 56) at `0x3bfe8be08890232de81017a98c8d39515d59978a`. Polygon/Arb/Op/Avax/Base still un-deployed. |
| UI | live | Debounced search, single long-lived SSE, throttled refetches, `React.memo`'d rows, AbortController on filter changes, Next.js error boundaries (`app/error.tsx`, `app/global-error.tsx`). |
| TG bot | live | `/forcerescue` command + relaxed long-poll error handling. |
| Force-rescue | live | API accepts `{ force, selectedSteps, selectedAssets }`; UI exposes step picker; bypasses verdict gate. |

### What's broken or missing

| Area | Symptom | Severity |
|---|---|---|
| Proxy-upgrade detection (`proxy.unprotected_implementation_upgrade`) | **0 findings out of 3,613.** Rule requires `facts_all: ["ERC-1967"]` and `min_role_confidence ≥ 0.9` — the Go auditor's proxy resolver gives up early (`unresolved_safe`) on almost every modern proxy, so the facts never get emitted. | **critical** — single biggest detection gap |
| Initializer-takeover (`init.public_initializer_takeover`) | **0 findings.** Same upstream cause. | critical |
| Reentrancy (`reentrancy.classic_mutex_absent`) | only 26 findings out of 3,613 — implausibly low. Rule's `flows_all` constraint is too tight. | high |
| Delegatecall risk (no rule) | We classify `delegatecall(_, _)` the same as `call(_, _)` even though DELEGATECALL = full storage takeover. | high |
| Approval-surface drain | `RESCUE_APPROVAL_CONSENT_MODE=scan-only` — we identify victims at risk but never drain (no consent registry yet). | high — biggest untapped $ surface |
| Bytecode-level drain mining | Only admin-NAMED selectors (`withdraw*`, `rescue*`, `sweep*`, `emergency*`, `claim*`) get tried. Non-named drainable selectors (`execute`, `dispatch`, `forward`, `process`, …) are missed. | medium |
| Multi-tx coordinated rescue | `executePlan` runs steps sequentially but doesn't model "step N+1 depends on step N's state change" (e.g. init-takeover then drain-as-new-owner). | medium |
| Flashloan provider coverage | Aave v3 only. Balancer / Morpho / Uniswap V3 flash all missing → BSC/Polygon/Arb economic findings unrescuable. | medium |
| Private mempool / MEV-share | None. We broadcast through public RPC → racing attackers. | high (operational) |
| Engine-version backfill | Manual via `POST /api/proofs/backfill`. Should auto-trigger on `ENGINE_VERSION` bump. | low |
| Rescue-success metric history | Not tracked. We don't know if a rules change regressed the drain rate. | low |

### Corpus-level numbers (as of 2026-05-17 19:00)

```
Total findings:                       3,613
Findings with PoE:                      425
True-positive rescues (drainable):        0
False positives (no real bug):         ~250  (mostly fee-on-transfer tax tokens)
Owner-only RUG-PULL findings:           134
"MISSED_DRAIN_SHAPE" (priced > $1):      18  (~$14k cumulative — Tier-1 target)
Dust-only contracts:                    ~95  (correctly short-circuited by v9)
OOM-killed audits (lifetime, pre-fix): ~200  (now fixed: 8 GB cap)
```

---

## 2 · Roadmap (do these in order)

### Tier 1 — moves the needle the most

#### 1.1 · Resurrect proxy-upgrade detection ★★★★★
Either relax the existing rule's prerequisites OR ship a runtime-shortcut rule that doesn't require the Go auditor's broken proxy resolver.

**Cheapest path** — new rule `proxy.upgradeTo_unprotected_v2`:
- Fires when bytecode contains the `0x3659cfe6 upgradeTo(address)` or `0x4f1ef286 upgradeToAndCall(address,bytes)` selectors.
- Suppressors: presence of `onlyOwner` / `onlyAdmin` modifier proof, OR a `OZ_OWNABLE2STEP` fact, OR a 2-of-N timelock guard.
- Severity: `critical`; expected confidence 0.85 with selector witness alone.

**Why this unlocks more than a new rule** — it feeds the existing `init.public_initializer_takeover` path. Init-takeover → write attacker into admin slot → call `upgradeTo(attackerImpl)` → drain on next call. We already have rescue-prove plumbing for this (`buildAdminFanout`), starved of inputs.

Files to touch:
- `rules/core/proxy.upgradeTo_unprotected_v2.json` (new)
- `panel/src/server/sim/exploits/proxy-upgrade.ts` (new verifier — selector probe + suppressor check)
- Wire in `panel/src/server/sim/worker.ts` (`runSidecarVerifiers`)
- Bump `ENGINE_VERSION` → 10
- Backfill: `POST /api/proofs/backfill?limit=3613&onlyMissing=false`

Expected impact: **0 → 50–200 new findings** with clear drain shapes.

#### 1.2 · Approval-surface consent mode ★★★★★
Switch from scan-only to live drain when consenting victims exist.

Mechanics:
1. **Consent registry**: SQLite table `victim_consents (victim_address, contract_address, signed_message, signature, expires_at)`. EIP-712 typed-data signature; victim authorises a single rescuer EOA to call `transferFrom(victim, escrow, ≤ allowance)` once.
2. **Approval scanner** (per finding): `eth_getLogs Approval(_, contract, _)` over deployment-block → latest; group by victim; balance + allowance check.
3. **`RESCUE_APPROVAL_CONSENT_MODE=auto`**: rescue-prove emits a per-victim drain step gated by registry presence. Live broadcaster refuses any victim without a valid consent row.

Files to touch:
- `panel/src/server/sim/rescue-prove.ts` (replace stub at `// v3-A: scan-only mode`)
- `panel/src/server/rescue/consent-registry.ts` (new)
- `panel/app/api/rescue/consent/route.ts` (new — POST signed consent)
- UI: per-finding "victim consent status" panel

Expected impact: catches the entire approval-drain class (≈ unbounded $ surface — depends on victim signups).

#### 1.3 · Bytecode-level drain mining ★★★★
For every verified finding with priced exposure, enumerate **all** PUSHed function selectors (we already extract them in `decon`), call each with attacker-favoured calldata variants `(escrow, balance, max_uint, [escrow, escrow])`, pre-flight-prune the misses.

Files to touch:
- `panel/src/server/sim/rescue-prove.ts` — new builder `buildBytecodeSelectorFanout` (sibling of `buildEvidenceAdminFanout`).
- Cap selector count per contract (~64) to keep fork load reasonable.

Expected impact: closes the existing **MISSED_DRAIN_SHAPE / 18 contracts / $14k** cluster.

#### 1.4 · Loosen reentrancy detection ★★★★
26/3,613 is impossibly low. Audit the `flows_all` constraint of `reentrancy.classic_mutex_absent`; allow weaker witness (e.g. `external_call_before_state_write` without requiring full mutex-absence proof).

Expected impact: **26 → 100–400** new findings, most with clear drain paths.

### Tier 2 — engine accuracy + execution robustness

| # | Item | Effort | Files |
|---|---|---|---|
| 2.1 | Engine-version auto-backfill on bump | small | `panel/src/server/sim/worker.ts`, new cron in `ecosystem.config.cjs` |
| 2.2 | Rescue-success metrics endpoint + dashboard tile | small | `panel/app/api/rescue/metrics/route.ts` (new), `panel/src/components/Dashboard.tsx` |
| 2.3 | Multi-tx coordinated rescue (state-dependency-aware) | medium | `panel/src/server/rescue/broadcaster.ts` (`executePlan` → DAG) |
| 2.4 | Delegatecall risk rule family | medium | `rules/core/delegatecall.*.json`, `panel/src/server/sim/exploits/delegatecall-takeover.ts` |
| 2.5 | EIP-712 / permit replay detection | medium | `rules/core/sig.permit_replay.json`, verifier |
| 2.6 | Upstream fee-on-transfer detector (skip wasted PoE cycles) | small | `panel/src/server/sim/rescue-prove.ts` (early-bail) + finding pre-classifier |

### Tier 3 — execution infrastructure

| # | Item | Effort | Notes |
|---|---|---|---|
| 3.1 | MEV-share / Flashbots Protect integration for ETH/Base/Op | medium-large | Replaces public-RPC broadcast for chains 1, 8453, 10. Critical to actually win drain races. |
| 3.2 | Owner-tracking watch list (for RUG-PULL findings, watch owner EOA) | medium | Adds a new ingest stream + ranking. |
| 3.3 | Balancer / Morpho / Uniswap-V3 flash loan providers | medium per provider | `panel/src/server/rescue/broadcaster.ts` + deploy a `FlashLoanRescue.sol` variant per provider. |
| 3.4 | Per-chain canonical flashloan receiver deployments | small each | Polygon, Arbitrum, Optimism, Avalanche, Base, Blast, Linea, Gnosis. Pattern: `forge create contracts/rescue/FlashLoanRescue.sol --rpc-url ... --private-key ...` then update `RESCUE_FLASHLOAN_RECEIVER` in `.env`. |
| 3.5 | L2 sequencer-aware bundle inclusion (force-include via L1) | large | Optimism/Base/Arb specific. |
| 3.6 | Bundle re-simulation against searcher tip (yield-share with MEV) | large | Unlocks competitive private-mempool wins. |

### Tier 4 — operational maturity

| # | Item | Effort |
|---|---|---|
| 4.1 | Push notifications on high-value findings (>$1k, >$10k, >$100k via TG) | small |
| 4.2 | Per-finding PoE history view (diff between engine versions) | small |
| 4.3 | Known-protocol denylist (Uniswap V3 pools, Aave receivers, Lido, Curve metapools) — skip re-flagging | small |
| 4.4 | Daily success-rate cron + regression alert | medium |
| 4.5 | Proper Go-auditor OOM fix (work scope is the Go repo — not this panel) | large |

### Tier 5 — paradigm shifts (months not weeks)

- SMT-based symbolic-execution backend replacing the Go auditor's custom CFG walker.
- Small LLM trained on disassembled bytecode → "drainability shape" classification.
- Federated PoE network with signed attestation (clone-vulnerability auto-detection across chains).
- On-chain rescue registry: time-locked vault that lets legitimate owners reclaim, with rescuer attestation. Resolves the legal/ethical ambiguity of unilateral rescues.

---

## 3 · Concrete first-day plan when resuming

Order of operations for the next session, designed to ship in a single working day:

```
[09:00]  Item 1.1 (proxy-upgrade rule + verifier)
         - write rules/core/proxy.upgradeTo_unprotected_v2.json
         - write panel/src/server/sim/exploits/proxy-upgrade.ts
         - wire into worker.ts
         - bump ENGINE_VERSION → 10
         - run on a small chain (linea-mainnet) as smoke test

[11:00]  Item 1.3 (bytecode selector fanout)
         - add buildBytecodeSelectorFanout to rescue-prove.ts
         - cap at 64 selectors per contract
         - unit test on the existing MISSED_DRAIN_SHAPE cluster

[13:00]  Backfill all 3,613 findings with engine v10
         - POST /api/proofs/backfill?limit=3613&onlyMissing=false
         - monitor RSS (new 8 GB cap)
         - measure new TP count

[16:00]  Item 1.4 (reentrancy loosening)
         - audit rule constraints
         - add "weak witness" variant
         - re-run sidecar on existing call.* findings

[18:00]  Push to GitHub: feat(detection): proxy-upgrade + selector fanout + reentrancy widening (v10)
         - measure: corpus FP/TP/missed-shape delta vs the baseline numbers in §1
```

Expected outcome at end of day-1: **50–400 new findings** (mostly from items 1.1 + 1.4), of which a meaningful fraction should drain successfully via the existing init-takeover plumbing and the new selector fanout (1.3).

---

## 4 · Environment & operational notes

### What's in `.env` (not committed)

Key vars touched this session:
- `RESCUE_FLASHLOAN_RECEIVER` — comma-separated `chainId:address` list. Currently has BSC.
- `RESCUER_PRIVATE_KEY` — for live broadcast.
- `RESCUE_AUTH_TOKEN` — server-side auth for `/rescue` API.
- `EVM_AUDIT_MAX_RSS_MB` — override the new 8 GB default if a contract still OOMs.
- `RESCUE_APPROVAL_CONSENT_MODE` — currently `scan-only`. Item 1.2 will make `auto` viable.

### PM2 process layout

`panel/ecosystem.config.cjs` was modified this session to explicitly re-read `.env` on every reload (via `loadDotenv()` helper), because `pm2 restart --update-env` was not propagating `.env` changes into the Next.js process env. **Don't revert this**.

### Files touched this session (full list)

- `bin/evm-audit-capped.sh` — RSS cap raise + peak-tracking diagnostics
- `panel/src/server/sim/rescue-prove.ts` — v9 (pre-fork short-circuit + evidence-driven admin fanout)
- `panel/src/server/rescue/broadcaster.ts` — envelope-aware flashloan pre-flight + BigInt-safe responses + WBNB borrow asset
- `panel/src/server/rescue/tg-bot.ts` — `/forcerescue` command
- `panel/ecosystem.config.cjs` — explicit `.env` reload on PM2 restart
- `panel/src/components/FindingsTable.tsx` — debounced search, throttled SSE, AbortController, `React.memo`
- `panel/src/components/Dashboard.tsx` — single-loop polling, AbortController, dedup SSE
- `panel/src/components/FindingDetail.tsx` — re-analyze button, force-rescue UI, verdict styling
- `panel/app/error.tsx` *(new)* — per-route Next.js error boundary
- `panel/app/global-error.tsx` *(new)* — root error boundary
- `panel/app/api/proofs/backfill/route.ts` *(new)* — bulk re-PoE endpoint
- `panel/app/api/proofs/[findingId]/rescue/route.ts` — accepts `force`, `selectedSteps`, `selectedAssets`
- `panel/app/api/simulation/route.ts` — extended rescue-eligible families (`init.*`, `economic.*`)
- `panel/src/server/sim/worker.ts` — same extension as above
- `contracts/rescue/FlashLoanRescue.sol` — already deployed to BSC; address recorded in `.env`
- `docs/ROADMAP_2026-05-17.md` *(this file)*

---

## 5 · Decision log (so next session understands the trade-offs)

- **RSS cap raised 2 GB → 8 GB rather than fixing the Go auditor.** The auditor's symbolic executor scales superlinearly with CFG size; properly streaming the CFG to disk is a multi-week change inside the Go repo. The 8 GB workaround is correct for now (host has 16+ GB; auditor is single-threaded).
- **Owner-side rescue removed (v5).** Per the policy shift earlier in the session, rescues are attacker-side only. Owner-impersonation via `anvil_impersonateAccount` is no longer in the v9 drain plan. Resurrect only if we adopt a custodial / signed-consent model.
- **`buildEvidenceAdminFanout` over a generic bytecode-selector fanout (v9).** Cheaper first cut. Item 1.3 in this roadmap extends it.
- **Flashloan envelope pre-flight (broadcaster.ts).** Instead of per-step fork checks (which Aave rejects with `address(0)` as the asset), we now simulate the entire `executeRescue()` call against a forked Aave Pool. WBNB resolved automatically from a `WRAPPED_NATIVE_BY_CHAIN_BROADCASTER` map.
- **Why no new TP rescues in the v9 backfill** despite the 3.2× runtime speedup: every remaining gap requires a *new* drain shape (approval-drain, selector-fanout) or *new* detection (proxy-upgrade, reentrancy widening). Tier-1 items address both.

---

_Last edited: 2026-05-17, end of session. Resume from §3._
